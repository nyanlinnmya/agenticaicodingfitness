# Week 19 — Fine-tuning on a DGX (interactive tutorial + real/simulated demos)
#
# Install into the repo's uv-managed .venv (Python 3.13):
#     uv pip install -r week19/dgx_finetune/requirements.txt
#
# The training LOOP is simulated (you can't train a 70B on a laptop). Dataset
# prep + recipe generation are pure-Python and always real. The before/after
# EVAL calls a live OpenAI-compatible endpoint if one is reachable (REAL mode),
# else it's stubbed (SIM). No cloud key, no cloud cost.
#   curl -fsSL https://ollama.com/install.sh | sh
#   ollama run qwen3.6:35b-a3b-q8_0      # or gemma4:12b on a Mac

openai>=1.40          # eval client, pointed at the local base_url (REAL mode)
fastapi>=0.110        # the tutorial web app's control plane
uvicorn>=0.29         # ASGI server for the web app
