# Copy the values you need into a real .env in the REPO ROOT (not here).
# The repo root .env is gitignored. NEVER commit real keys.

ANTHROPIC_API_KEY="sk-ant-...your key..."

# Optional — only used by a couple of lessons / the class repo examples:
OPENROUTER_API_KEY=""
ZAI_API_KEY=""
SERPER_API_KEY=""
