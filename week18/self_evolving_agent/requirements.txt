# Week 18 — Self-Evolving Agent with Persistent Memory
#
# The ENGINE (episodic SessionDB, semantic fencing, the SKILL.md library, GC,
# GEPA) is pure Python stdlib — checkpoints 1–7 run with NO API key and NO
# network. Only the LIVE visualizer (real LLM agent turns + real meta-cognitive
# consolidation) needs the Claude Agent SDK + the `claude` CLI.

# ── the interactive web apps (server.py + tutorial_server.py) ───────────────
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
httpx>=0.27                   # tutorial_server health checks + run streaming

# ── LIVE mode only (real LLM) ───────────────────────────────────────────────
# The SDK drives the local `claude` CLI, which uses your Claude Code subscription
# auth — NO ANTHROPIC_API_KEY required (an unrelated key would be rejected).
#     npm install -g @anthropic-ai/claude-code   # then run `claude` once to sign in
claude-agent-sdk>=0.1.50

# ── optional: vector semantic memory (Part 4.5 VectorMemoryStore) ───────────
# chromadb>=0.5                # embed + retrieve only query-relevant facts
