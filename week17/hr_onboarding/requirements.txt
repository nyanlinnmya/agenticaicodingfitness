# Week 17 — HR onboarding: a long-running ADK agent with persistent sessions.
#
# Unlike the week17/checkpoints (offline demos), this is a REAL, runnable ADK
# service. It needs one model credential. This repo ships an ANTHROPIC_API_KEY,
# so by default the agent runs on Claude via ADK's LiteLlm. To use Gemini (the
# blog's model) instead, set GOOGLE_API_KEY and ONBOARDING_MODEL=gemini-2.0-flash.

google-adk>=2.0          # Agent, Runner, DatabaseSessionService, FunctionTool
litellm>=1.40            # lets ADK run on Claude (anthropic/…) instead of Gemini
greenlet>=3.0            # required by SQLAlchemy async (DatabaseSessionService)
aiosqlite>=0.20          # the sqlite+aiosqlite:// async driver

# the long-running service + the demo client
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6

# install:  uv pip install -r week17/hr_onboarding/requirements.txt
