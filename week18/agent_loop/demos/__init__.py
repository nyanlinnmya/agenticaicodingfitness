"""Runnable agent-loop demos for the Week 18 tutorial (Claude Agent SDK).

Each ``stepNN_*.py`` is a self-contained script that makes REAL SDK calls and
narrates the agent loop via ``loopview.run_loop``. Run them in order, or click
through them in the tutorial web app (``tutorial_server.py``).
"""
