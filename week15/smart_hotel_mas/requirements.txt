# --- Core (CP1, CP2) ---
neo4j>=5.14.0
python-dotenv>=1.0.0

# --- L3 Semantic memory (CP3) ---
chromadb>=0.5.0
sentence-transformers>=2.2.0      # local embeddings, no API key

# --- Optimization + forecasting (CP4) ---
pulp>=2.7.0
prophet>=1.1.0
pandas>=2.0.0
numpy>=1.24.0

# --- RL + anomaly detection (CP5) ---
gymnasium>=0.29.0
stable-baselines3>=2.0.0
scikit-learn>=1.3.0

# --- Multi-agent + GraphRAG (CP6, graphrag/) ---
crewai>=0.55.0
crewai-tools>=0.8.0
langchain>=0.2.0
langchain-anthropic>=0.2.0
langchain-community>=0.2.0
anthropic>=0.40.0

# --- Production API (production/) ---
fastapi>=0.110.0
uvicorn>=0.27.0
redis>=5.0.0
